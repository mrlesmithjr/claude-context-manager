List recent observations captured by context-manager for the current project.

Run this command and display the results:
```bash
node ~/.claude/plugins/context-manager/dist/cli.js list --project "$PWD" --limit 20
```

Format the output as a readable list showing the observation summaries, tools used, and timestamps.
