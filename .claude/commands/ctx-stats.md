Show context-manager statistics for the current project.

Run this command and display the results:
```bash
node __homedir__/Projects/Personal/claude-context-manager/dist/cli.js stats --project "$PWD"
```

Summarize the output showing: total observations, sessions, tokens, and date range.
